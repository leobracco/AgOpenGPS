<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="PilotX.Desktop.Views.HubPanel">
    <!--
        HubPanel - reemplazo nativo de pages/hub.html (home del Hub).
        KPIs (velocidad/rumbo/dosis/secciones/posicion/lote) se actualizan
        desde el HudSnapshot que ya viene a 4Hz, sin red propia. La lista
        de nodos se refresca a 3s contra /api/nodos/unified (reusa
        NodosClient). Los toggles de widgets QX/VX/FX hablan a
        /api/overlays (FormGPS los relee cada 250ms desde JSON y aplica
        en caliente).

        Card flotante (no takeover): el fondo vive en este Border con
        bordes redondeados, y el mapa sigue vivo detrás — antes el
        Background iba en el UserControl a pantalla completa y con el
        mapa apagado era "una página negra" (reporte 2026-08-11).
    -->
    <!-- Variante Light: los controles que no llevan color propio (barras de
         scroll, botones sin pintar) salen del FluentTheme, y la app arranca en
         Dark; adentro de la card tienen que verse claros. -->
    <ThemeVariantScope RequestedThemeVariant="Light">
    <!-- Name="Card": ModoEmbebido() (adentro de la Configuración) le
         suelta el marco/tamaño; la instancia flotante no cambia. -->
    <Border Name="Card"
            Background="{StaticResource PilotXPanelCard}"
            BorderBrush="{StaticResource PilotXPanelBorderHigh}" BorderThickness="1"
            CornerRadius="14" ClipToBounds="True"
            BoxShadow="0 8 26 0 #33101612">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <StackPanel Name="ContenidoRaiz" Margin="24,20,24,24" Spacing="16">

            <!-- ============ Header + pills ============ -->
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Name="HeaderTitulo" Grid.Column="0" Spacing="2">
                    <TextBlock Text="Hub"
                               Foreground="{StaticResource PilotXPanelText}"
                               FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock Text="Estado en tiempo real del sistema"
                               Foreground="{StaticResource PilotXPanelTextDim}"
                               FontSize="12"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="8"
                            VerticalAlignment="Center">

                    <!-- Pill: trabajo -->
                    <Border Name="PillJob"
                            Background="{StaticResource PilotXPanelSurface2}"
                            BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                            BorderThickness="1" CornerRadius="999"
                            Padding="12,6,12,6">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Ellipse Name="DotJob" Width="8" Height="8"
                                     Fill="{StaticResource PilotXPanelTextDim}"/>
                            <TextBlock Name="TxtJob"
                                       Text="Sin trabajo"
                                       Foreground="{StaticResource PilotXPanelTextMid}"
                                       FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <!-- Pill: broker -->
                    <Border Name="PillBroker"
                            Background="{StaticResource PilotXPanelSurface2}"
                            BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                            BorderThickness="1" CornerRadius="999"
                            Padding="12,6,12,6">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Ellipse Name="DotBroker" Width="8" Height="8"
                                     Fill="{StaticResource PilotXPanelTextDim}"/>
                            <TextBlock Name="TxtBroker"
                                       Text="Broker MQTT"
                                       Foreground="{StaticResource PilotXPanelTextMid}"
                                       FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <!-- Pill: nodos -->
                    <Border Name="PillNodos"
                            Background="{StaticResource PilotXPanelSurface2}"
                            BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                            BorderThickness="1" CornerRadius="999"
                            Padding="12,6,12,6">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Ellipse Name="DotNodos" Width="8" Height="8"
                                     Fill="{StaticResource PilotXPanelTextDim}"/>
                            <TextBlock Name="TxtNodos"
                                       Text="0 nodos"
                                       Foreground="{StaticResource PilotXPanelTextMid}"
                                       FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- ============ Grid 4 KPI cards ============ -->
            <UniformGrid Columns="2" Rows="2">

                <!-- Velocidad / Rumbo -->
                <Border Margin="0,0,8,8"
                        Background="{StaticResource PilotXPanelSurface}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1" CornerRadius="8"
                        Padding="16,14,16,14">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Velocidad"
                                   Foreground="{StaticResource PilotXPanelTextDim}"
                                   FontSize="12" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Bottom">
                            <TextBlock Name="KpiSpeed"
                                       Text="0.0"
                                       Foreground="{StaticResource PilotXPanelText}"
                                       FontSize="34" FontWeight="Bold"/>
                            <TextBlock Text="km/h"
                                       Foreground="{StaticResource PilotXPanelTextDim}"
                                       FontSize="14"
                                       VerticalAlignment="Bottom"
                                       Margin="0,0,0,7"/>
                        </StackPanel>
                        <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,0">
                            <TextBlock Grid.Column="0" Text="Rumbo"
                                       Foreground="{StaticResource PilotXPanelTextDim}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Name="KpiHeading" Text="— °"
                                       Foreground="{StaticResource PilotXPanelText}"
                                       FontSize="13" FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Dosis / Shape -->
                <Border Margin="8,0,0,8"
                        Background="{StaticResource PilotXPanelSurface}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1" CornerRadius="8"
                        Padding="16,14,16,14">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Dosis actual"
                                   Foreground="{StaticResource PilotXPanelTextDim}"
                                   FontSize="12" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Bottom">
                            <TextBlock Name="KpiDose"
                                       Text="—"
                                       Foreground="{StaticResource PilotXPanelText}"
                                       FontSize="34" FontWeight="Bold"/>
                            <TextBlock Text="kg/ha"
                                       Foreground="{StaticResource PilotXPanelTextDim}"
                                       FontSize="14"
                                       VerticalAlignment="Bottom"
                                       Margin="0,0,0,7"/>
                        </StackPanel>
                        <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,0">
                            <TextBlock Grid.Column="0" Text="Shapefile"
                                       Foreground="{StaticResource PilotXPanelTextDim}" FontSize="12"/>
                            <Border Grid.Column="1" Name="ShapePill"
                                    Background="{StaticResource PilotXPanelSurface2}"
                                    BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                                    BorderThickness="1" CornerRadius="999"
                                    Padding="8,2,8,2">
                                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                    <Ellipse Name="DotShape" Width="6" Height="6"
                                             Fill="{StaticResource PilotXPanelTextDim}"/>
                                    <TextBlock Name="TxtShape" Text="sin shape"
                                               Foreground="{StaticResource PilotXPanelTextMid}" FontSize="11"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Secciones -->
                <Border Margin="0,8,8,0"
                        Background="{StaticResource PilotXPanelSurface}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1" CornerRadius="8"
                        Padding="16,14,16,14">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Secciones"
                                   Foreground="{StaticResource PilotXPanelTextDim}"
                                   FontSize="12" FontWeight="SemiBold"/>
                        <StackPanel Name="SectionsRow"
                                    Orientation="Horizontal"
                                    Spacing="4"
                                    HorizontalAlignment="Stretch"
                                    MinHeight="34"/>
                        <Grid ColumnDefinitions="*,Auto" Margin="0,2,0,0">
                            <TextBlock Grid.Column="0" Name="KpiSecCount" Text="0 de 0 abiertas"
                                       Foreground="{StaticResource PilotXPanelTextDim}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Name="KpiToolWidth" Text="— m"
                                       Foreground="{StaticResource PilotXPanelText}"
                                       FontSize="13" FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Posición / Lote -->
                <Border Margin="8,8,0,0"
                        Background="{StaticResource PilotXPanelSurface}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1" CornerRadius="8"
                        Padding="16,14,16,14">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Posicion"
                                   Foreground="{StaticResource PilotXPanelTextDim}"
                                   FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Name="KpiLatLon" Text="—"
                                   Foreground="{StaticResource PilotXPanelText}"
                                   FontSize="20" FontWeight="SemiBold"
                                   FontFamily="Consolas, Courier New, monospace"
                                   TextWrapping="Wrap"/>
                        <Grid ColumnDefinitions="*,Auto" Margin="0,4,0,0">
                            <TextBlock Grid.Column="0" Text="Lote activo"
                                       Foreground="{StaticResource PilotXPanelTextDim}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Name="KpiField" Text="—"
                                       Foreground="{StaticResource PilotXPanelText}"
                                       FontSize="13" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="160"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- ============ Overlays toggles ============ -->
            <TextBlock Text="Widgets sobre el mapa"
                       Foreground="{StaticResource PilotXPanelText}"
                       FontSize="16" FontWeight="SemiBold"
                       Margin="0,4,0,0"/>
            <Border Background="{StaticResource PilotXPanelSurface}"
                    BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                    BorderThickness="1" CornerRadius="8"
                    Padding="16,14,16,14">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Grid.Column="0" Spacing="2">
                        <TextBlock Text="Overlays de PilotX"
                                   Foreground="{StaticResource PilotXPanelText}"
                                   FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="Activa o desactiva en caliente cada widget que se ve sobre el mapa principal."
                                   Foreground="{StaticResource PilotXPanelTextDim}"
                                   FontSize="11"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <Button Name="BtnOvQX"
                                Content="⟁ QuantiX"
                                Click="OnToggleQx"
                                MinHeight="36"
                                Padding="14,6,14,6"
                                FontSize="13" FontWeight="SemiBold"
                                Background="{StaticResource PilotXPanelAccent}"
                                Foreground="{StaticResource PilotXPanelText}"
                                BorderBrush="{StaticResource PilotXPanelAccent}"
                                BorderThickness="1"
                                CornerRadius="6"/>
                        <Button Name="BtnOvVX"
                                Content="◉ VistaX"
                                Click="OnToggleVx"
                                MinHeight="36"
                                Padding="14,6,14,6"
                                FontSize="13" FontWeight="SemiBold"
                                Background="{StaticResource PilotXPanelAccent}"
                                Foreground="{StaticResource PilotXPanelText}"
                                BorderBrush="{StaticResource PilotXPanelAccent}"
                                BorderThickness="1"
                                CornerRadius="6"/>
                        <Button Name="BtnOvFX"
                                Content="◊ FlowX"
                                Click="OnToggleFx"
                                MinHeight="36"
                                Padding="14,6,14,6"
                                FontSize="13" FontWeight="SemiBold"
                                Background="{StaticResource PilotXPanelAccent}"
                                Foreground="{StaticResource PilotXPanelText}"
                                BorderBrush="{StaticResource PilotXPanelAccent}"
                                BorderThickness="1"
                                CornerRadius="6"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ============ Nodos ============ -->
            <TextBlock Text="Nodos detectados"
                       Foreground="{StaticResource PilotXPanelText}"
                       FontSize="16" FontWeight="SemiBold"
                       Margin="0,4,0,0"/>
            <StackPanel Name="NodosHost" Spacing="6"/>

            <!-- ============ Acciones rapidas ============ -->
            <TextBlock Text="Acciones rapidas"
                       Foreground="{StaticResource PilotXPanelText}"
                       FontSize="16" FontWeight="SemiBold"
                       Margin="0,4,0,0"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Name="BtnGoQuantix"
                        Content="Abrir QuantiX"
                        Click="OnGoQuantix"
                        MinHeight="40"
                        Padding="18,8,18,8"
                        FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource PilotXPanelAccent}"
                        Foreground="{StaticResource PilotXPanelText}"
                        BorderBrush="{StaticResource PilotXPanelAccent}"
                        BorderThickness="1"
                        CornerRadius="6"/>
                <Button Name="BtnGoVistax"
                        Content="Abrir VistaX"
                        Click="OnGoVistax"
                        MinHeight="40"
                        Padding="18,8,18,8"
                        FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource PilotXPanelSurface2}"
                        Foreground="{StaticResource PilotXPanelText}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1"
                        CornerRadius="6"/>
                <Button Name="BtnGoNodos"
                        Content="Ver nodos"
                        Click="OnGoNodos"
                        MinHeight="40"
                        Padding="18,8,18,8"
                        FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource PilotXPanelSurface2}"
                        Foreground="{StaticResource PilotXPanelText}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1"
                        CornerRadius="6"/>
                <!-- CoreX-ECU: el comando corex_ecu existía pero NINGÚN botón
                     lo disparaba desde el Hub nativo — quedó huérfano en el
                     port (reporte 2026-08-07 probando el ECU real). -->
                <Button Name="BtnGoCorexEcu"
                        Content="CoreX-ECU"
                        Click="OnGoCorexEcu"
                        MinHeight="40"
                        Padding="18,8,18,8"
                        FontSize="14" FontWeight="SemiBold"
                        Background="{StaticResource PilotXPanelSurface2}"
                        Foreground="{StaticResource PilotXPanelText}"
                        BorderBrush="{StaticResource PilotXPanelBorderHigh}"
                        BorderThickness="1"
                        CornerRadius="6"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
    </Border>
    </ThemeVariantScope>
</UserControl>
